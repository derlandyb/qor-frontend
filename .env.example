VITE_API_BASE_URL=http://localhost:8080
VITE_MAPBOX_TOKEN=
# Google Identity Services client id — leave blank to disable the Google sign-in button
# entirely (no script load, no network call); see GoogleSignInButton's own scope note.
VITE_GOOGLE_CLIENT_ID=
